<h1>coucou</h1>
<%= simple_form_for :search, url: recap_path, method: "GET" do |f|  %>
  <%= f.label :transports %>
  <%= f.input :transports, input_html: {name: 'Bus[transports]'}, as: :boolean, checked_value: true, unchecked_value: false %>
  <%= f.submit "Résumé" %>
<% end %>

<%# Copier coller et modifier title, nom de la case à cocher et catégorie pour chaque point d'intéret %>
