<h1>coucou</h1>
<%= simple_form_for :search, url: tester_path, method: "GET" do |f|  %>
  <%= f.label :transports %>
  <%= f.input :transports, input_html: {name: 'toto[transports]'}, as: :boolean, checked_value: true, unchecked_value: false %>

  <%= f.submit "go" %>
<% end %>

<%# Copier coller et modifier title, nom de la case à cocher et catégorie %>
